Adminer has been deployed successfully!

{{- if .Values.virtualService.enabled }}

Access Adminer via Istio VirtualService:
{{- range .Values.virtualService.hosts }}
  https://{{ . }}
{{- end }}

Make sure the Istio gateway is configured and DNS is pointing to your ingress.

{{- else if .Values.ingress.enabled }}

Access Adminer via Ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}

{{- else if contains "NodePort" .Values.service.type }}

Get the application URL by running:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "adminer.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT

{{- else if contains "LoadBalancer" .Values.service.type }}

Get the application URL by running:
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "adminer.fullname" . }}

{{- else if contains "ClusterIP" .Values.service.type }}

Get the application URL by running:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "adminer.fullname" . }} 8080:{{ .Values.service.port }}
  
Then open http://localhost:8080 in your browser.

{{- end }}

{{- if .Values.database.driver }}

Default database driver: {{ .Values.database.driver }}
{{- end }}
{{- if or .Values.database.server .Values.config.externalserver }}

Default database server: {{ .Values.database.server | default .Values.config.externalserver }}
{{- end }}
